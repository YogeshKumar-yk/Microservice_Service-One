package com.service.one.service;

import org.springframework.context.annotation.Configuration;
import org.springframework.web.servlet.config.annotation.CorsRegistry;
import org.springframework.web.servlet.config.annotation.WebMvcConfigurer;
//@Configuration
//public class cross implements WebMvcConfigurer {
//
//    @Override
//    public void addCorsMappings(CorsRegistry registry) {
//
//            registry.addMapping("/**")  // Specify the path pattern to which this CORS configuration applies
//                    .allowCredentials(true)  // Allow credentials like cookies, authorization headers, etc.
//                    .allowedHeaders("*")  // Allow all headers in the request
//                    .allowedMethods("GET", "POST", "PUT", "DELETE")  // Allow specific HTTP methods
//                    .allowedOrigins("*");  // Allow requests from any origin. You can specify specific origins instead of "*"
//        }
//
//}
